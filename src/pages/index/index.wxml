<!--index.wxml-->
<view class="container">
  <view class="swiper">
    <swiper indicator-dots="true" autoplay="true" >
      <block wx:for="{{boards[0].movies.subjects}}">
        <swiper-item>
          <image src="{{item.images.large}}" class="slide-image"/>
        </swiper-item>
      </block>
    </swiper>
  </view>

</view>
